[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "agent"
version = "1.0.0"

requires-python = ">=3.10"
dependencies = [
    "openai",
    "requests",
]

[tool.setuptools.packages.find]
exclude = []
